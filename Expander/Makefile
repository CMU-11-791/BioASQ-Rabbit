NAME=expander

include ../Makefile.master

download:
	if [ ! -e metamap.tgz ] ; then wget http://downloads.lappsgrid.org/downloads/metamap.tgz ; fi
	if [ ! -e pymetamap.tgz ] ; then wget http://downloads.lappsgrid.org/downloads/pymetamap.tgz ; fi
